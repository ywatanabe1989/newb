🐝 newb: probing https://github.com/ywatanabe1989/newb.git ...
{
  "package": "repo",
  "what_for": "**newb** is a tool that verifies Python package documentation by running a fresh AI agent that reads only your docs and attempts to use your package, reporting whether the documentation is sufficient for a newcomer.",
  "problems_solved": "| # | Problem | Solution |\n|---|---------|----------|\n| 1 | Developers don't know if their package documentation is accurate and usable by new users | newb automatically tests documentation by having a fresh AI agent read your docs and attempt to use the package |\n| 2 | Hard to verify documentation quality in CI/CD pipelines | newb provides JSON and markdown output formats suitable for CI integration and automated reporting |\n| 3 | Need to ensure documentation answers key questions (what for, problems solved, quick start, when not to use) | newb asks four canonical questions automatically and supports custom tests via tests_newb.yaml |\n| 4 | Documentation testing requires safe isolation to prevent side effects on the host system | newb runs agents in isolated environments (host subprocess, docker, or apptainer) with restricted tool access (Read only) |\n| 5 | Testing documentation manually is time-consuming and doesn't scale across versions | newb automates documentation testing with configurable runtime options and comprehensive grading (substring matching + LLM judges) |",
  "quick_start": "```python\nimport newb\nreport = newb(\"./docs\")\nprint(newb.render_markdown(report))\n```",
  "when_not_to_use": "Based on my review of the three .md files in /work/skills, the documentation for the `newb` package (and its aliases `agentic-test` and `newbie-test`) does not explicitly state when someone should NOT use the package. The README covers what the package does (having a fresh AI agent verify your package documentation works) and how to use it, but omits guidance on inappropriate use cases."
}
🐝 smoke check complete (no tests_newb.yaml found)
