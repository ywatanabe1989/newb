{
  "package": "newb",
  "what_for": "This package is a documentation verifier that tests whether a package's docs are sufficient for an AI agent to understand and use the package by spinning up a sandboxed Claude agent to read the docs and answer canonical questions about its purpose, problems solved, quick start, and limitations.",
  "problems_solved": "| # | Problem | Solution |\n|---|---|---|\n| 1 | Package documentation starts in the middle without clear framing | newb's `what_for` question verifies that docs clearly state the package's purpose in one sentence |\n| 2 | No problem-to-solution mapping; docs list features instead of explaining value | newb's `problems_solved` question ensures docs map 3-5 problems to their solutions |\n| 3 | Quick start examples are missing or buried in scattered docs | newb's `quick_start` question verifies a minimal working example is easily discoverable |\n| 4 | Package boundaries and limitations are undocumented | newb's `when_not_to_use` question ensures docs explicitly describe when NOT to use the package |\n| 5 | Inconsistent doc quality prevents fresh agents from independently learning package usage | newb spins up a sandboxed agent to verify documentation is sufficient for complete newcomers to understand and use the package |",
  "quick_start": "```python\nimport newb\n\n# Bare-module callable (PEP 562) \u2014 equivalent to newb.run(...)\nreport = newb(\"./src/mypkg/_skills/mypkg\")\nprint(report[\"what_for\"])\n\n# Explicit form (mirrors pytest.main convention)\nreport = newb.run(\"./docs\", model=\"claude-haiku-4-5\", runs_per_prompt=1)\n\n# Render the markdown form for paste-into-README:\nprint(newb.render_markdown(report))\n```",
  "when_not_to_use": "Based on the documentation, here's the answer:\n\n**Do NOT use newb to run unit tests, measure code coverage, benchmark model reasoning, perform security testing, grade prose quality, or verify that code examples actually execute**\u2014newb verifies only whether your docs are sufficient for an agent to understand your package, never executing code or testing model capabilities themselves. For those jobs, use pytest, pytest-cov, inspect-ai, red-team frameworks, vale, and doctest respectively."
}
