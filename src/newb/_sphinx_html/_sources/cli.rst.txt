CLI reference
=============

Rendered from the live Click group via ``sphinx-click`` — this tree
cannot drift from ``newb --help-recursive``.

``newb <target>`` is the canonical, verb-less invocation (pytest-style);
the subcommands below are introspection / maintainer plumbing.

.. click:: newb._cli:main
   :prog: newb
   :nested: full
